<ul>
    {{#each items}}
    <li>
        <h5>{{this.name}}</h5>
        <audio controls src="{{this.preview_url}}" type='audio/mpeg'>
            Your browser does not support the
            <code>audio</code> element.
        </audio>
    </li>
    {{/each}}
</ul>